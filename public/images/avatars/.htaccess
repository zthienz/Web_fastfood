# Chỉ cho phép truy cập file ảnh
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Chặn truy cập các file khác
<FilesMatch "^(?!.*\.(jpg|jpeg|png|gif)$).*$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Chặn listing directory
Options -Indexes